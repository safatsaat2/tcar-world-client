import { useLoaderData } from "react-router-dom";

const SingleToyPage = () => {
    const datas = useLoaderData();
    console.log(datas)
    return (
        <div>
            
        </div>
    );
};

export default SingleToyPage;